{
  "name": "readout-e2e",
  "private": true,
  "description": "Playwright e2e suite for readout, driven against the fakeapi harness (make e2e)",
  "devDependencies": {
    "@playwright/test": "1.60.0"
  }
}
